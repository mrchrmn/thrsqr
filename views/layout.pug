doctype html 

html(lang="en-UK")

  head 
    block title
      title There or Square 
    meta(charset="UTF-8")
    meta(name="author" content="Marc Hermann")
    meta(name="description" content="RSVP app for weekly events.")
    script(src="/javascript/application.js")

  body 
    header 
      block headerlinks 
        a(href="/") Home

    main 
    - let msgTypes = Object.keys(flash || {});
    if msgTypes.length > 0
    ul 
      each msgType in msgTypes 
        each message in flash[msgType] 
          li.flash(class=type)= message 

    block main 